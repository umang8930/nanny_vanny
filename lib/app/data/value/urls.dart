import 'package:nanny_vanny/app/data/value/evn.dart';

class URLs {
  URLs._privateConstructor();

  // static const logIn = "${Env.baseURL}/auth/login/mobile";
  // static const verifyOTP = "${Env.baseURL}/auth/login/verifyOTP";
  // static const uploadFile = "${Env.baseURL}/uploadFile";
  static const currentBookingList = "${Env.baseURL}/current_booking_list";
  static const packagesList = "${Env.baseURL}/packages_list";
}
